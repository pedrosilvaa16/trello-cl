/*
  Ver a nota em src/app/convite/[token]/constantes.ts: um módulo "use server"
  só exporta funções assíncronas, por isso o tipo do estado vive aqui.
*/

export type EstadoEntrada = { erro?: string };
